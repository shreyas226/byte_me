# GeoChat-7B 4-bit Inference Working Environment Requirements
# Verified functional on Windows 11 with NVIDIA RTX 4060 Laptop GPU (8GB VRAM)
# Python 3.10

--find-links https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Core 4-bit Quantization & HuggingFace Ecosystem Pins
# IMPORTANT: DO NOT UPGRADE accelerate OR transformers beyond these exact pins
# Newer accelerate (>0.21.0) breaks 4-bit dispatch with `.to()` calls on quantized weights
# Newer transformers (>4.31.0) breaks GeoChat architectural model definitions (_expand_mask)
bitsandbytes==0.50.1
accelerate==0.21.0
transformers==4.31.0
huggingface_hub==0.16.4
tokenizers==0.13.3
sentencepiece==0.2.2
protobuf==7.36.0
einops==0.8.2
requests==2.34.2
pillow==12.3.0
shortuuid==1.0.13
markdown2==2.5.5
